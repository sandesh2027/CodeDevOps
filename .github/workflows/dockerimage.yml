name: Docker Image CI

on: [push]

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag addressbook:v0.2
    - name: Docker credential
      run: docker login docker.io --username=sandesh2026 --password=DreamGoal#2030
    - name: docker push
      run: docker push addressbook:v0.2
      
